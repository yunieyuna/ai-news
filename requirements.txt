# Core
pyyaml>=6.0
python-dotenv>=1.0.0

# Gather (RSS + full-article extraction)
feedparser>=6.0.0
httpx>=0.25.0
trafilatura>=1.6.0

# Analyze (Ollama uses OpenAI client with base_url; Groq/OpenAI optional)
openai>=1.0.0
groq>=0.4.0

# Notify (SMTP = free)
# stdlib smtplib + email; no extra deps

# Web UI (optional)
flask>=3.0.0
markdown>=3.5.0

# Optional: local DB later
# aiosqlite>=0.19.0
